# Dependências de desenvolvimento e do CI. A imagem de produção instala só api/requirements.txt.
# A suíte precisa também de pytest, do mlflow (sete arquivos de teste importam o
# train_pipeline, que importa mlflow) e do ruff (lint mínimo: só o que quebra em execução).
# Os pinos são os do ambiente onde a suíte foi validada (pip freeze, 14/09/2026).
-r api/requirements.txt
pytest==9.1.1
mlflow==3.16.0
ruff==0.16.7
